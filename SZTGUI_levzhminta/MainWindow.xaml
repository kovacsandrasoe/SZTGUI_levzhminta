<Window x:Class="SZTGUI_levzhminta.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SZTGUI_levzhminta"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" ItemsSource="{Binding Foods}">
            
        </ListBox>
        <StackPanel Grid.Column="1">
            <Button Margin="10" Content="Kosárba" />
            <Button Margin="10" Content="Törlés" />
        </StackPanel>
        <ListBox Grid.Column="2" ItemsSource="{Binding Basket}">

        </ListBox>
    </Grid>
</Window>
